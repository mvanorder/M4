<%= bootstrap_form_for @auth_group do |f| %>
  <% if @auth_group.errors.any? %>
    <div id="error_explination">
      <h2>
        <%= pluralize(@auth_group.errors.count, "error") %> prohibited this auth_group form being saved:
      </h2>
      <ul>
        <% @auth_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :name, label: "Name" %>
    <%= f.text_field :description, label: "Description" %>
    <%= f.submit %>

<% end %>
